#include <bits/stdc++.h>
using namespace std;
#define ll long long
int main()
{
    int t;
    cin>>t;
    while (t--)
    {
        int n,q;
        cin>>n>>q;
        vector<pair<ll,ll>> v(n);
        for (int i = 0; i < n; i++)
        {
            int x;
            cin>>x;
            v[i]={x,log2(x)};
        }
        vector<ll> a(q);
        for (int i = 0; i < q; i++)
        {
            cin>>a[i];
        }
        for (int i = 0; i < q; i++)
        {
            
        }
        
         
                
    }
    
}