#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while (t--)
    {
      int size;
      cin>>size;
      vector<int> vec(size);
      for (int i = 0; i < size; i++)
      {
        int temp;
        cin>>temp;
        vec.push_back(temp);
      }
      vector<int> cpy(size),ans(size);
      cpy=vec;
      sort(cpy.begin(),cpy.end());
      for (int i = 0; i < size; i++)
      {
        if()
      }
      
    
      

        
    }
    
}